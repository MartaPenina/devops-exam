# Ansible configuration file - defines default settings for all playbook runs

[defaults]
host_key_checking = False
remote_user = root
private_key_file = ~/.ssh/devops-exam

[ssh_connection]
ssh_args = -o StrictHostKeyChecking=no